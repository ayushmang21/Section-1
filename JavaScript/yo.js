

for (let i = 10; i <= 200; i++)
{
    // let sqrt = i ** 0.5;
    // let n = sqrt * sqrt;
    if (Math.sqrt(i) == parseInt(Math.sqrt(i)))
    {
        console.log(i + " - is Perfect Square");
    }
}